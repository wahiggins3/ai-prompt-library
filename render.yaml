services:
  - type: web
    name: prompt-library-api
    env: node
    runtime: node
    nodeVersion: 18.x
    buildCommand: |
      cd server
      npm ci
      npm install openai@^4.0.0
    startCommand: NODE_ENV=production node index.js
    dir: server

