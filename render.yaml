services:
  - type: web
    name: prompt-library-api
    env: node
    buildCommand: npm install && cd server && npm install && npm install openai@^4.0.0
    startCommand: NODE_ENV=production node server/index.js

